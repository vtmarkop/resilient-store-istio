apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: store-gateway
  namespace: default
spec:
  gatewayClassName: istio
  listeners:
  - name: http
    port: 80
    protocol: HTTP
    # hostname is intentionally omitted to accept all traffic
    allowedRoutes:
      namespaces:
        from: Same